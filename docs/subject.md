# Inception

> **Summary:** This document is a System Administration related exercise.
> **Version:** 5.2

---

## Table of Contents

1. [Preamble](#i-preamble)
2. [Introduction](#ii-introduction)
3. [General Guidelines](#iii-general-guidelines)
4. [AI Instructions](#iv-ai-instructions)
5. [Mandatory Part](#v-mandatory-part)
6. [Readme Requirements](#vi-readme-requirements)
7. [Prerequisites for Validation](#vii-prerequisites-for-validation)
8. [Bonus Part](#viii-bonus-part)
9. [Submission and Peer-Evaluation](#ix-submission-and-peer-evaluation)

---

## I — Preamble

*(No additional content.)*

---

## II — Introduction

This project aims to broaden your knowledge of system administration by using Docker.
You will virtualize several Docker images, creating them in your new personal virtual machine.

---

## III — General Guidelines

- This project needs to be done on a **Virtual Machine**.
- All the files required for the configuration of your project must be placed in a `srcs` folder.
- A `Makefile` is also required and must be located at the root of your directory. It must set up your entire application (i.e., it has to build the Docker images using `docker-compose.yml`).
- This subject requires putting into practice concepts that, depending on your background, you may not have learned yet. We advise you to read a lot of documentation related to Docker usage, as well as anything else you will find helpful in order to complete this assignment.

---

## IV — AI Instructions

### Context

During your learning journey, AI can assist with many different tasks. Take the time to explore the various capabilities of AI tools and how they can support your work. However, always approach them with caution and critically assess the results. Whether it's code, documentation, ideas, or technical explanations, you can never be completely sure that your question was well-formed or that the generated content is accurate. Your peers are a valuable resource to help you avoid mistakes and blind spots.

### Main Message

- Use AI to **reduce repetitive or tedious tasks**.
- Develop **prompting skills** — both coding and non-coding — that will benefit your future career.
- Learn how AI systems work to better anticipate and avoid common risks, biases, and ethical issues.
- Continue building both technical and power skills by **working with your peers**.
- Only use AI-generated content that you **fully understand** and can take responsibility for.

### Learner Rules

- Take the time to explore AI tools and understand how they work, so you can use them ethically and reduce potential biases.
- Reflect on your problem before prompting — this helps you write clearer, more detailed, and more relevant prompts using accurate vocabulary.
- Develop the habit of systematically **checking, reviewing, questioning, and testing** anything generated by AI.
- Always seek **peer review** — don't rely solely on your own validation.

### Phase Outcomes

- Develop both general-purpose and domain-specific prompting skills.
- Boost your productivity with effective use of AI tools.
- Continue strengthening computational thinking, problem-solving, adaptability, and collaboration.

### Examples

| | Practice |
|---|---|
| ✅ **Good** | I ask AI: *"How do I test a sorting function?"* It gives me a few ideas. I try them out and review the results with a peer. We refine the approach together. |
| ❌ **Bad** | I ask AI to write a whole function, copy-paste it into my project. During peer-evaluation, I can't explain what it does or why. I lose credibility — and I fail my project. |
| ✅ **Good** | I use AI to help design a parser. Then I walk through the logic with a peer. We catch two bugs and rewrite it together — better, cleaner, and fully understood. |
| ❌ **Bad** | I let Copilot generate my code for a key part of my project. It compiles, but I can't explain how it handles pipes. During the evaluation, I fail to justify and I fail my project. |

---

## V — Mandatory Part

This project consists of having you set up a small infrastructure composed of different services under specific rules. The whole project has to be done in a virtual machine using **docker compose**.

### Rules

- Each Docker image must have the **same name** as its corresponding service.
- Each service has to run in a **dedicated container**.
- Containers must be built from the **penultimate stable version of Alpine or Debian**.
- You must write your own `Dockerfile` per service. The Dockerfiles must be called in your `docker-compose.yml` by your `Makefile`.
- It is **forbidden** to pull ready-made Docker images or use DockerHub (Alpine/Debian excluded).

### Required Services

- A container with **NGINX** using TLSv1.2 or TLSv1.3 only.
- A container with **WordPress + php-fpm** (installed and configured), without nginx.
- A container with **MariaDB** only, without nginx.
- A **volume** for the WordPress database.
- A **volume** for the WordPress website files.
  - Both named volumes must store their data inside `/home/login/data` on the host machine.
  - Bind mounts are **not allowed**.
- A **docker-network** connecting all containers.

> Containers must **restart automatically** in case of a crash.

### Important Restrictions

> A Docker container is not a virtual machine. Do not use hacky patches based on `tail -f` and similar methods. Read about how daemons work.

- `network: host`, `--link`, and `links:` are **forbidden**.
- The `network` line must be present in your `docker-compose.yml`.
- Containers must **not** be started with infinite-loop commands. Prohibited: `tail -f`, `bash`, `sleep infinity`, `while true`.
- Read about **PID 1** and best practices for writing Dockerfiles.

### WordPress Database

- There must be **two users**, one being the administrator.
- The administrator's username **cannot** contain `admin`, `Admin`, or `administrator` (e.g., `admin`, `administrator`, `Admin-login`, `admin-123` are all forbidden).

### Domain Name

- Configure your domain name to point to your local IP address.
- Domain must be: `login.42.fr` (replace `login` with your own login).

### Security

- The **latest** tag is prohibited.
- **No password** must be present in your Dockerfiles.
- It is mandatory to use **environment variables**.
- A `.env` file is mandatory to store environment variables.
- It is strongly recommended to use **Docker secrets** for confidential information.
- Any credentials, API keys, or passwords found in your Git repository will result in **project failure**.

### Entry Point

> Your NGINX container must be the **only entry point** into your infrastructure via **port 443 only**, using TLSv1.2 or TLSv1.3.

### Expected Directory Structure

```
.
├── Makefile
├── secrets/
│   ├── credentials.txt
│   ├── db_password.txt
│   └── db_root_password.txt
└── srcs/
    ├── docker-compose.yml
    ├── .env
    └── requirements/
        ├── bonus/
        ├── mariadb/
        │   ├── Dockerfile
        │   ├── .dockerignore
        │   ├── conf/
        │   └── tools/
        ├── nginx/
        │   ├── Dockerfile
        │   ├── .dockerignore
        │   ├── conf/
        │   └── tools/
        ├── tools/
        └── wordpress/
```

### Example `.env`

```env
DOMAIN_NAME=wil.42.fr
# MYSQL SETUP
MYSQL_USER=XXXXXXXXXXXX
```

> For obvious security reasons, any credentials, API keys, passwords, etc., must be saved locally and **ignored by git**. Publicly stored credentials will lead directly to a failure of the project.

---

## VI — Readme Requirements

A `README.md` file must be provided at the root of your Git repository.

### Required Content

- The **first line** must be italicized and read: *This project has been created as part of the 42 curriculum by \<login\>.*
- A **Description** section: project goal and brief overview.
- An **Instructions** section: compilation, installation, and/or execution.
- A **Resources** section: references (docs, articles, tutorials) + description of how AI was used.

### Project Description Section (Required)

Must also explain the use of Docker and the sources included in the project, including:

| Comparison |
|---|
| Virtual Machines vs Docker |
| Secrets vs Environment Variables |
| Docker Network vs Host Network |
| Docker Volumes vs Bind Mounts |

> The README must be written in **English**.

---

## VII — Prerequisites for Validation

The following documentation files must be present at the root of your repository in Markdown format:

### `USER_DOC.md` — User Documentation

Must explain, in clear and simple terms, how an end user or administrator can:

- Understand what services are provided by the stack.
- Start and stop the project.
- Access the website and the administration panel.
- Locate and manage credentials.
- Check that the services are running correctly.

### `DEV_DOC.md` — Developer Documentation

Must describe how a developer can:

- Set up the environment from scratch (prerequisites, configuration files, secrets).
- Build and launch the project using the Makefile and Docker Compose.
- Use relevant commands to manage the containers and volumes.
- Identify where the project data is stored and how it persists.

---

## VIII — Bonus Part

A `Dockerfile` must be written for each additional service. Each service runs in its own container with a dedicated volume if necessary.

### Bonus List

- Set up **Redis cache** for your WordPress website.
- Set up a **FTP server** container pointing to the WordPress website volume.
- Create a simple **static website** in any language except PHP.
- Set up **Adminer**.
- Set up a **service of your choice** (must be justified during the defense).

> The bonus part will **only be assessed** if the mandatory part is completed **perfectly** and without any malfunctions.

---

## IX — Submission and Peer-Evaluation

Submit your assignment in your Git repository. Only the work inside the repository will be evaluated during the defense.

- Double-check the names of your folders and files.
- During the evaluation, a **brief modification** of the project may occasionally be requested (a minor behaviour change, a few lines of code, or an easy-to-add feature).
- This step is meant to verify your **actual understanding** of the project.
- The modification should be feasible within a few minutes.
